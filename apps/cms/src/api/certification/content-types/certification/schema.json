{
  "kind": "collectionType",
  "collectionName": "certifications",
  "info": {
    "singularName": "certification",
    "pluralName": "certifications",
    "displayName": "Certification",
    "description": "Quality certification shown on /o-nemocnici — from DATA_MODEL.md aboutInfo.certifications. Certificate numbers and issuing bodies are load-bearing facts; keep them in sync with the hospital's own records."
  },
  "options": { "draftAndPublish": true },
  "pluginOptions": { "i18n": { "localized": true } },
  "attributes": {
    "name": { "type": "string", "required": true },
    "desc": { "type": "text", "required": true, "pluginOptions": { "i18n": { "localized": true } } }
  }
}
